SQLAlchemy>=2.0,<2.1
alembic>=1.13
python-dotenv>=1.0

# Scraper: Playwright drives a real headless Chromium (Seek is JS-rendered).
# After installing, run once to fetch the browser binary:
#   python -m playwright install chromium
playwright>=1.40

# Local API backend for the Chrome extension (ingest + dashboard endpoints).
fastapi>=0.110
uvicorn[standard]>=0.29
aiofiles>=23.0   # required by FastAPI StaticFiles
httpx>=0.27  # used by tests / for hitting the API
pytest>=8.0  # test runner

# LLM layer: all calls go through app/llm/client.py; provider/model/key from .env.
# Gemini (Google GenAI SDK) — kept for fallback / future use.
google-genai>=1.0
# Groq (OpenAI-compatible SDK) — active provider (llama-3.3-70b-versatile).
groq>=0.9
# Makes Python's TLS use the OS trust store — needed where AV/corporate proxies
# do TLS interception (the local CA is in the OS store, not certifi's bundle).
truststore>=0.9

# Postgres driver — uncomment when deploying with a postgresql:// DATABASE_URL.
# psycopg[binary]>=3.1
